// // Arrow Function
// const createArray = <X, Y>(param1: X, param2: Y): [X, Y] => {
//   return [param1, param2];
// };

// function createArray2<X, Y>(param1: X, param2: Y): [X, Y] {
//   return [param1, param2];
// }

// const result1 = createArray<string, string>("Bangladesh", "I love my Country");
// const result2 = createArray<boolean, Array<string>>(true, ["USA"]);

// type Name = { name: string };

// const result3 = createArray<Name, boolean>({ name: "Bd" }, false);

// // spread operator

// // const newData = { ...myInfo, crush };
// const addMeInMyCrushMind = (myInfo: object) => {
//   const crush = "Selim Molla";
//   const newData = { ...myInfo, crush };
//   return newData;
// };

// const myInfo = {
//   name: "Razibul",
//   age: 31,
//   salary: 10000,
// };

// const result = addMeInMyCrushMind(myInfo);
